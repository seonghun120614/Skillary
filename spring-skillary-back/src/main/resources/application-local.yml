spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testdb
    username: test
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

jwt:
  secret: "this-is-very-secret-key-for-jwt-signing-please-change-it"
  access-expire-milli-seconds: 1800000
  refresh-expire-milli-seconds: 604800000

cookie:
  secure: true
  http-only: true
  same-site: "Strict"
  path: "/"

#jwt:
#  secret: ${JWT_SECRET_KEY}
#  access-expire-time: ${JWT_EXPIRE_TIME_ACCESS}
#  refresh-expire-time: ${JWT_EXPIRE_TIME_REFRESH}